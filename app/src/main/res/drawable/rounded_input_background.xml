<?xml version="1.0" encoding="utf-8"?>
<!-- Luminous Finance "glass well" — recessed input surface, darker than the
     GlassCardView it sits inside, with a faint translucent border. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/color_bg" />
    <stroke android:width="1dp" android:color="@color/color_glass_border" />
    <corners android:radius="16dp" />
</shape>
