<?xml version="1.0" encoding="utf-8"?>
<!-- Preference/Data row per the "Vivid Glass Variant": bg-white/5, border-white/5, rounded-2xl
     (16dp) — the standard airy translucent chip look shared with the rest of the app. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/color_glass_fill_l2" />
    <corners android:radius="16dp" />
    <stroke android:width="1dp" android:color="@color/color_glass_border_faint" />
</shape>
