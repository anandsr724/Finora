<?xml version="1.0" encoding="utf-8"?>
<!-- Import CSV button per "Vivid Glass Variant": bg-white/10 border-white/10, rounded-2xl
     (16dp) — same weight as Export but neutral, not brand-tinted. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/color_white_10" />
    <corners android:radius="16dp" />
    <stroke android:width="1dp" android:color="@color/color_white_10" />
</shape>
